# Neon / Postgres connection string used by the API (api/index.js).
# When set, the challenges and submissions endpoints read from and write to Postgres.
# When unset, the API falls back to api/data.json.
NEON_DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"
